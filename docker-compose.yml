services:
  postgres:
    image: postgres:18-alpine
    environment:
      POSTGRES_DB: pulse
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5433:5432"
    volumes:
      - pulse-postgres-data:/var/lib/postgresql

volumes:
  pulse-postgres-data:
